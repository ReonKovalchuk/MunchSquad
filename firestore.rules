rules_version = '2';

service cloud.firestore {
	match /databases/{database}/documents{
		match /{collectionID}/{recipeID} {
			allow read, write: if 
      (request.auth == null && resource.data.uid == '') ||
      (request.auth != null && resource.data.uid == request.auth.uid)
		}
  }
}